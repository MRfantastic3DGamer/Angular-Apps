name: Better-Tree Workflow

on:
  push:
    branches:
      - main
      - master

jobs:
  better-tree-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Better-Tree Action
        uses: your-username/better-tree-action@v1
        with:
          root: "~/work/Angular-Apps/Angular-Apps/project/app"
          doc: "~/work/Angular-Apps/Angular-Apps/project/README.md"
          no-files: "false"
          stack-folders: "false"
          show-hidden: "false"
          ignored-locations: "gradle/"
